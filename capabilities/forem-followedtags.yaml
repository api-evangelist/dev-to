naftiko: 1.0.0-alpha2
info:
  label: Dev.to Forem API — FollowedTags
  description: 'Dev.to Forem API — FollowedTags. 1 operations. Lead operation: Followed Tags. Self-contained Naftiko capability
    covering one Dev To business surface.'
  tags:
  - Dev To
  - FollowedTags
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DEV_TO_API_KEY: DEV_TO_API_KEY
capability:
  consumes:
  - type: http
    namespace: forem-followedtags
    baseUri: https://dev.to/api
    description: Dev.to Forem API — FollowedTags business capability. Self-contained, no shared references.
    resources:
    - name: follows-tags
      path: /follows/tags
      operations:
      - name: getfollowedtags
        method: GET
        description: Followed Tags
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: api-key
      value: '{{env.DEV_TO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: forem-followedtags-rest
    port: 8080
    description: REST adapter for Dev.to Forem API — FollowedTags. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/follows/tags
      name: follows-tags
      description: REST surface for follows-tags.
      operations:
      - method: GET
        name: getfollowedtags
        description: Followed Tags
        call: forem-followedtags.getfollowedtags
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: forem-followedtags-mcp
    port: 9090
    transport: http
    description: MCP adapter for Dev.to Forem API — FollowedTags. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: followed-tags
      description: Followed Tags
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: forem-followedtags.getfollowedtags
      outputParameters:
      - type: object
        mapping: $.
